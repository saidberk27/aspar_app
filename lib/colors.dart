import 'package:flutter/material.dart';

class ProjectColors {
  static const Color darkBlue = Color(0xFF166FC0);
  static const Color lightBlue = Color(0xFF0FA9EA);
  static const Color white = Colors.white;

  static const MaterialColor succesColor = Colors.green;
  static const MaterialColor failColor = Colors.red;
  static const MaterialColor yellow = Colors.yellow;
}
